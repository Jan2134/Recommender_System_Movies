pandas>=2.0
numpy>=1.24
scipy>=1.10
scikit-learn>=1.3
matplotlib>=3.7
streamlit>=1.30

# Only needed to run notebooks locally (not used by main.py / run_experiments.py / the app)
jupyter>=1.0

# Optional: library cross-check for matrix factorization
# scikit-surprise>=1.1.4
